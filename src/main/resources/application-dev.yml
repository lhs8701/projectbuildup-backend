spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mivv_dev?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: root
    password: fromhaedal22!
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-test-query: select 1 from dual

swagger:
  enable: true
